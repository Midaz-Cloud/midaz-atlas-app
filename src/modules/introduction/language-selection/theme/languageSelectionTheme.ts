import { introductionLayout } from '../../theme';

/** P2 language cards — Figma Kiosco Midaz. */
export const languageSelectionColors = {
  iconBgSpanish: '#fff7ed',
  iconBgEnglish: '#eff6ff',
} as const;

export const languageSelectionLayout = {
  flagWidth: introductionLayout.flagWidth,
  flagHeight: introductionLayout.flagHeight,
} as const;
