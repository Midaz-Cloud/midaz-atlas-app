<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug: allow HTTP to loopback (adb reverse) and emulator host.
  The kiosk often cannot initiate Wi-Fi client-to-PC TCP (AP isolation);
  local API then uses http://localhost:3000 + adb reverse tcp:3000.
  Release keeps main/res config (cleartext only to loopback for HkaApp).
  Do not hardcode lab LAN IPs here — point KIOSK_* URLs at localhost and reverse.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
